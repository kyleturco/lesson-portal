.login-background
  .container
    .register.col-md-6
      h2 Login
      p Already a user, get rockin!
      form(class="form" ng-submit="login()")
        .form-group
            input.form-control(type="text" name="username" placeholder="Enter Username" ng-model="loginForm.username" required)
        .form-group
          input.form-control(type="password" name="password" placeholder="Password" ng-model="loginForm.password" required)
        button.btn.btn-default(type="submit" ng-disabled="disabled") Submit
        a(href='/')
          button.btn.btn-primary(type="button") Cancel
